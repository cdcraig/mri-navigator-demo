# MRI Navigator — interactive demo

Single self-contained HTML page. No build step, no framework, no backend,
no network calls at runtime. Open `index.html` in any browser.

All patient data, findings, measurements and report language are **invented**
for demonstration. Nothing here is medical advice.

Deployed as a Render static site (publish path `.`, no build command).
